services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        APP: api
    ports:
      - '3001:3001'
    env_file:
      - path: .env

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        APP: web
    ports:
      - '3000:3000'
    depends_on:
      - api
    env_file:
      - path: .env
