enum RoleCategory {
  FRONTEND
  BACKEND
  MOBILE
  DEVOPS
  DATA
}

enum ResourceType {
  YOUTUBE
  DOCS
  COURSE
  ARTICLE
}

enum NodeType {
  GROUP
  MILESTONE
  REQUIRED
  OPTIONAL
}

enum NodeStatus {
  LOCKED
  IN_PROGRESS
  COMPLETED
}

enum UserRole {
  USER
  ADMIN
}
